{% extends 'base.html.twig' %}

{% block body %}
    <h1>Appel_d_offre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appel_d_offre.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ appel_d_offre.status }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('appel_d_offre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('appel_d_offre_edit', { 'id': appel_d_offre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
